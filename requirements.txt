# Core LLM providers (Anthropic is the default; OpenAI is a drop-in fallback)
anthropic>=0.40.0
openai>=1.50.0

# Data validation, structured output, and configuration
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# HTTP + resilience
requests>=2.32.0
tenacity>=8.4.0

# Testing
pytest>=8.2.0
